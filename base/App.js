import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import AppRoot from './src/app_code/AppRoot';

export default class App extends React.Component {
  render() {
    return (
      <AppRoot></AppRoot>
    );
  }
}